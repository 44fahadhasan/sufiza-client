import Button from "../../reusable/Button";

const NavEnd = () => {
  return (
    <div>
      {/* login button */}
      <button type="button" className="flex">
        <Button label={"Login"} />
      </button>
    </div>
  );
};

export default NavEnd;
